# FORGE X Configuration Environment

# Moss Low-Latency Retrieval Layer (Optional: Falls back to in-process sub-10ms Moss engine)
MOSS_PROJECT_ID=
MOSS_PROJECT_KEY=

# LLM Provider Configuration (Optional: Deterministic fallback provider active by default)
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
LLM_MODEL=gpt-4o-mini

# Server Port Settings
PORT=8000
FRONTEND_PORT=5173
HOST=0.0.0.0
